/* Empty stand-in so that legacy/main.cpp, which includes <windows.h> without
 * using anything from it, compiles on non-Windows hosts for the head-less
 * regression build.  Only on the include path of tools/legacy_shim.cpp. */
